package BitWise;

public class BitWise {

    public static void main(String[] args) {
        // 0 -> false 1 -> true

        // BITWISE MANIPULATION

        // OPERATORS
        // 1. AND & -> when you &1 with any number digits remains same

        // 2. OR | -> either true = true

        // 3. XOR ^ exclusive OR
        // -> a ^ 1 = ~a
        // -> a ^ 0 = a
        // -> a ^ a = 0

        // 4. Complement (~) --> oposite

        // NUMBER SYSTEM

        // 1. Decimal (base10) 0 to 9

        // 2. Binary (base2) 0 and 1

        // 3. Octal (base8) 0 to 8

        // 4. Hexadecimal (base16) 0 to 9 to A-F

        // COVERSION

        // 1. Decimal to base b -> keep dividing by b, take remainders in opposite

        // 2. base b to deciaml -> multiply and add the power of base with digits

        // MORE OPERATORS

        // 1. Left shift operator (<<) --> shifts digits left by one
        // 1010 << 1 = 10100
        // a << b = a * 2^b

        // 2. Right shift operator (>>) --> shifts digits right by one
        // 0011001 >> 1 = 001100
        // a >> b = a / 2^b

    }
}
